<%- include('./layouts/header') %> 

<h1>Category</h1>

<table class="table">
    <thead>
        <th>ID</th>
        <th>Category</th>
        <th>Description</th>
    </thead>
    <tbody>
        <% categories.forEach(category=> { %>
            <tr>
                <td>
                    <%= category.id %>
                </td>
                <td>
                    <%= category.name %>
                    </a>
                </td>
                <td>
                    <%= category.description %>
                </td>
            </tr>
            <% }) %>
    </tbody>
</table>

<%- include('./layouts/footer') %> 